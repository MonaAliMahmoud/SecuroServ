<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.securoserv">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".application.AppInstance"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SecuroServ">

        <activity
            android:name="com.securoserv.ui.splash.SplashActivity"
            android:theme="@style/Theme.SecuroServ.NoStatusBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.securoserv.ui.signup.SignUpActivity"
            android:theme="@style/Theme.SecuroServ.NoStatusBar"
            android:exported="false" />

        <activity
            android:name="com.securoserv.ui.login.LoginActivity"
            android:theme="@style/Theme.SecuroServ.NoStatusBar"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.SecuroServ.NoActionBar"
            android:exported="false">
        </activity>

    </application>

</manifest>